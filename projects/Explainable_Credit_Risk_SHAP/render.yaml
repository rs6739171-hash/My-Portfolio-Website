services:
  - type: web
    name: explainable-credit-risk-shap
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python train_model.py
    startCommand: python serve.py
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
